<?php
    /** @var \MageSuite\ProductTile\Block\Tile\Container $this */
    $layout = $this->getLayout();
    $htmlTag = !empty($block->getHtmlTag()) ? $block->getHtmlTag() : 'div';
    $cssClass = $block->getCssClass() ? 'class="'.$block->getCssClass().'"' : '';

    $output = '';

    foreach($this->getChilds() as $child) {
        $child->setTile($block->getTile());
        $output .= $layout->renderElement($child->getNameInLayout(), false);
    }

    // Prevent container from being rendered if it's empty
    if (!trim($output)) { return; }
?>
<<?= $htmlTag ?> <?= $cssClass ?>><?=$output?></<?= $htmlTag ?>>
